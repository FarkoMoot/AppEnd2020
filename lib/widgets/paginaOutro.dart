import 'package:flutter/material.dart';

class paginaOutro extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Pagina outro carregada com sucesso!!!"),
    );
  }
}
