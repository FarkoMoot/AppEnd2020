import "package:flutter/material.dart";

class paginaTodo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Pagina TODO carregada com sucesso!!!'),
    );
  }
}
